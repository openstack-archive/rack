my_int_if: eth1
my_ext_if: eth2
my_mng_if: eth3

my_int_ip: "{{ ansible_eth1.ipv4.address }}"
my_ext_ip: "{{ ansible_eth2.ipv4.address }}"
my_mng_ip: "{{ ansible_eth3.ipv4.address }}"

my_int_obj: "{{ ansible_eth1 }}"
my_ext_obj: "{{ ansible_eth2 }}"
my_mng_obj: "{{ ansible_eth3 }}"
